import { Component } from '@angular/core';

@Component({
  standalone: true,
  selector: 'app-customers',
  template: `<h2>Customers</h2>
  <p>Liste der Kunden (Platzhalter).</p>`
})
export class CustomersComponent {}
